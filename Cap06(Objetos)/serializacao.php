<html>

<head>
	<meta charset="UTF-8">
</head>

<?php
/*
 * $encoded = serialize(something);
 * $something = unserialize(encoded);
 * 
 * Métodos __sleep e __wakeup() são chamados ao antes de serializar ou depois de deserializar o objeto
 * 
 * __sleep: Deve retornar um array contendo os nomes dos atributos que devem ir para o stream de bytes. Array vazio
 * não grava nenhum dado.
 * 
 * __wakeup: Livre pra fazer o que quiser.
 */
?>

</html>